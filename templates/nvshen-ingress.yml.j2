---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: "nvshen"
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: "immissi.cn"
      http:
        paths:
          - backend:
              serviceName: nvshen
              servicePort: 80
            path: /
    - host: "immissi.com"
      http:
        paths:
          - backend:
              serviceName: nvshen
              servicePort: 80
            path: /
    - host: "youyanvren.com.cn"
      http:
        paths:
          - backend:
              serviceName: nvshen
              servicePort: 80
            path: /
